#! /bin/bash

set -ueo pipefail

tempfile=$(tempfile -p $(basename $0))
cherry HEAD $* > $tempfile

if test -s $tempfile; then
  tig --stdin --no-walk --reverse < $tempfile
else
  echo "All commits merged, good job!"
fi
