#! /bin/sh -e

block_file=$(git rev-parse --show-cdup).gitcherryblock

commit=$1

echo $commit >> $block_file
git add $block_file
git show -s --pretty=format:"Block commit %h: %s%n%ncommit %H%nAuthor: %an%nDate:   %ad%n" $commit | git commit -F -
